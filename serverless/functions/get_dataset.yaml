handler: bydelsfakta_api.handler.handler
events:
- http:
    path: /{dataset}
    method: get
    cors: true
    request:
      parameters:
        querystrings:
          geography: false
        paths:
          dataset: true
    documentation: ${file(serverless/documentation/get_dataset.yaml)}
